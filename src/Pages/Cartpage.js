import React from 'react';

const Cartpage = ({ cart }) => {
  

  return (
    <div>
      <h2>Your Shopping Cart</h2>
      {/* {isEmpty ? <EmptyCart /> : <FilledCart />} */}
    </div>
  );
};

export default Cartpage;
